- **巨大な数値を送るとサーバーエラー（500）になっていた穴を塞いだ。** レーティングや月数などに 10 の 400 乗のような数を入れると、計算の途中で溢れてエラーになっていました。入力の整数は ±1 兆までとし、それを超える値は入力エラー（422）として断ります。 読み込んだ .chum5 / Foundry VTT のファイルに手書きで巨大な数が入っていたときは、断るのではなく上限まで丸めて読み込みます。
