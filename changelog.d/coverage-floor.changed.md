- **テストのカバレッジに下限を入れ直した。** frontend の閂を実測（statements 83.6 / branches 67.0 / lines 85.3）の少し下へ上げ、backend には下限が無かったので `fail_under = 92`（実測 94.1）を入れました。目標値ではなく、テストの無い機能が入ったり、通すためにテストが消されたときに CI が気づくための閂です。
